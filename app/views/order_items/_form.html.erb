<%= form_with(model: order_item) do |form| %>
  <% if order_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
        <% order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.collection_select :order_id,
                  @orders,
                  :id,
                  :id,
                  prompt: "Select an order" %>  </div>


  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.collection_select :item_id,
                  @items,
                  :id,
                  :id,
                  prompt: "Select an item" %>  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
