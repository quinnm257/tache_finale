<%= form_with(model: store) do |form| %>
  <% if store.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
        <% store.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :store_name, style: "display: block" %>
    <%= form.textarea :store_name %>
  </div>

  <div>
    <%= form.label :adress, style: "display: block" %>
    <%= form.textarea :adress %>
  </div>

  <div>
    <%= form.label :phone, style: "display: block" %>
    <%= form.textarea :phone %>
  </div>

  <div>
    <%= form.label :country_id, style: "display: block" %>
    <%= form.text_field :country_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
